// 1.使用for循环、forEach() 、map()方法都可以遍历一个数组，他们之间有什么区别？
// for循环要定义一个变量，不单纯针对数组，forEach只能针对数组，map针对数组，生成一个新数组

// 2.谈谈你对DOM的理解
// DOM文档对象模型，就是页面的所有元素，都可以用document来指向

// 3.谈谈你对作用域的理解
/* 作用域分为全局作用域、局部作用域、块级作用域；
全局作用域针对全局有效，
局部作用域针对一个局部的值有效，比如函数，循环等，
块级作用域，通常由let 和{ } 产生块级作用域 */

// 4.箭头函数与一般函数的区别？
// 没有arguments，但是有rest隐藏参数，写法...变量名

// 5.什么是闭包？闭包的作用是什么？
// 一个函数内部的变量，用来跨作用域进行访问的东西
// 作用: 保护函数内部的变量, 不受全局变量的影响, 同时形成一个块级作用域
